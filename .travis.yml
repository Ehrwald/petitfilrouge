dist: bionic
language: java
jdk:
  - oraclejdk11
cache:
  directories:
    - .autoconf
    - $HOME/.m2

addons:
      sonarcloud:
        organization: "Ehrwald"
        token:
          secure: "$SONAR_TOKEN"
script:
  - mvn jetty:run &
  - sleep 10s
  - curl 127.0.0.1:8080
